# 2026-08-28

## 项目代码重构（可读性 / 可维护性）

按 AGENTS.md 的「架构决策门禁」执行：**只做函数抽取与重复消除，不引入 trait 层 / 新抽象**。

### 后端（Rust）
- `lib.rs::run` 222 行 → 拆出 `init_logging_system` / `bootstrap` / `init_home_dir` /
  `init_trace_manager` / `auto_start_gateway_if_enabled` / `spawn_settings_watchdog`。
  剩余 120 行里约 57 行是 `invoke_handler` 命令注册清单（声明式，无需拆）。
- `dispatch_upstream.rs::handle_sse_response` 205 行 → 后台读取循环抽为
  `run_sse_read_task` + `process_sse_events`（引入 `ChunkOutcome::{Data, End}`）。
- `dispatch_upstream.rs::handle_nonstreaming_aggregate` 147 行 → 抽为
  `collect_and_aggregate` + `collect_sse_event` + `finish_aggregation`。
  顺带删掉两个未使用的入参 `_headers` / `_config`。
- `transform_request.rs::run` 115 行 → 拆为 `inject_stream_options` /
  `inject_claude_default_max_tokens` / `convert_to_outbound_protocol` /
  `apply_upstream_request` + `edit_body_checked` 错误包装辅助。
- `plugins/mod.rs`：3 个插件重复「解析 body → 改 → 序列化回写」，统一走新增的
  `RequestContext::edit_body`；`compress_text` 拆为
  `looks_like_terminal_log` / `is_log_line` / `collapse_middle` / `compress_message_content`，
  魔法数字全部提为 `const`。
- `config_transfer_service.rs`：`import_config` 164→78 行，三段内联 SQL 抽为
  `replace_all_with_bundle` / `insert_providers` / `insert_model_mappings` /
  `insert_model_mapping_channels`；`restore_settings_from_backup_db` 101→20 行，
  抽为 `load_settings_from_backup_db` / `read_gateway_settings_row`；
  两处重复的「同步内存状态」合并为 `apply_gateway_settings`。
- **消除 usage 解析双实现**：`stream_response.rs` 新增 `SseEvent::parse_usage()`，
  `context.rs` 新增 `StreamSharedState::record_usage()`，流式与非流式两条路径共用。
- `StageError.context` 改为 `Box<RequestContext>`：Err 变体 1456B → 指针大小，
  清掉 8 条 clippy `result_large_err`。仅 3 处访问点（全在 pipeline.rs）需改。

### 前端
- 新增 `src/utils/health.ts`（`healthStatusType` / `healthStatusLabel`），
  消除 ModelSquareView 与 ProvidersView 各自实现的健康状态映射。
- `formatPrice` / `formatTokens` 从 ModelSquareView 移入既有 `src/utils/format.ts`。

### 结果
- `cargo test -p silk --lib`：100 passed / 0 failed。
- clippy：我改动的文件告警清零；剩余 9 条均在未触碰文件（auto_detect /
  stream_response 测试 / prism_wasm），属存量。
- `vue-tsc --noEmit` 通过；`vite build` 产物正常。

### 注意事项
- **本仓 CRLF**：`lib.rs`、`config_transfer_service.rs` 等为 CRLF 换行，
  Edit 工具精确匹配会失败，需改用 Python 按行号替换（`newline=""` 读写）。
- 工作树里 `api/index.ts`、`settings_service.rs`、`persist_log.rs`、
  `stores/*` 存在**既有的未提交改动**（非本次重构产生）。
- `vite build` 清空 `dist/` 时会被沙箱 safe-delete 拦截，可改用
  `npx vite build --outDir dist-verify` 绕开。

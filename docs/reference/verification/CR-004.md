# CR-004 验收记录（feat/CR-004）

> 变更范围：预设模块新增 OMP 模型管理能力（models.yml providers.<id> 段写入/剥离/导入 + omp CLI 模型探测 + 前端结构化模型编辑器）。
> 验收时间：2026-09-12 · 验收方式：fst-review（N6）· 分支：`feat/CR-004`（498bec8）
> 变更单：`docs/cr/CR-004-preset-omp-model-management.md`（已归档 1d8aaa0）
> 过程态产物：`.agent-workplace/iterations/current/`（dod-checklist / gray-release / retrospective / tech-debt）

## 1. DoD 核销清单

| # | 项 | 必须 | 结果 | 证据 |
|---|---|---|---|---|
| 1 | 功能完成（含骨架冒烟通过） | ✅ | ✅ | 3 批次 11/11 task 完成；OmpWriter 写/剥离/导入/回显集成测试 4 用例全绿；前端 ompSpec + model-defs 编辑器 |
| 2 | 变更针对性测试通过 | ✅ | ✅ | omp 6 用例 + preset_providers 6 用例 + preset_service 导入用例全绿；模型探测降级验证；影响点清单 8 项逐项核对 |
| 3 | 核心主干回归通过 | ✅ | ✅ | `cargo check` ✅；`cargo test` 164/164 ✅；`bun run build`（vue-tsc + vite）✅ |
| 4 | 文档同步（PRD/设计文档） | ✅ | ✅ | CR-004 变更单已归档；本验收记录定稿；影响评估 7 个影响点全部核对 |
| 5 | 变更记录归档 | ✅ | ✅ | `docs/cr/CR-004-preset-omp-model-management.md` 已提交（1d8aaa0）；分支提交带 CR id（498bec8） |
| 6 | 待定需求不纳入交付验收 | — | ✅ | thinking_level_map / config.yml 联动 / cost contextOver200k 分级均明确不纳入本次（用户确认） |

## 2. 变更针对性测试（改动点 + 关联影响点）

- **OmpWriter 写合并**：未管理 provider 保留、新 provider 段写入、`_silk_managed` 标记、结构化模型元数据（contextWindow 等）保留、未声明字段不凭空出现。
- **OmpWriter 剥离**：仅删本 provider 段，其他 provider 与未管理条目保留（快照回滚）。
- **extract_* 导入**：启动导入全部 providers 段、按 stored 提取单段、`_silk_managed` 剥离。
- **模型探测**：`omp --list-models` 表格解析（thinking/images 列）；未安装/超时降级为空列表不报错。
- **注册与能力表**：`writer_for("omp")`、`AgentType::all`、`HARNESS_NATIVE_PROTOCOLS`、网关注入（baseUrl/apiKey）、预设默认值。
- **前端**：`ompSpec` toSettings/fromSettings 无损往返；`model-defs` 编辑器渲染；OMP 探测导入（listOmpModels）。
- **真实配置安全**：Gate6 首轮事故（测试污染真实 ~/.omp/agent/models.yml）已闭环——测试用 `PI_CODING_AGENT_DIR` 环境变量隔离 + ENV_LOCK 串行化，真实文件已恢复无污染，技术债登记 TD-004-3。

## 3. 核心回归

| 检查 | 结果 |
|---|---|
| `cargo check -p silk` | ✅ |
| `cargo test`（lib 164） | ✅ 全部通过 |
| `bun run build`（vue-tsc + vite） | ✅ |
| CR-004 验收边界 1-6 | ✅ 全部满足 |

## 4. 灰度方案（N7）

桌面单机应用，无灰度流量概念。放量决策等价于**验收通过 → 合并 `feat/CR-004` → main → 构建发布**。回滚预案：普通 merge，必要时 `git revert`；新增 agent_type 为增量注册，不创建 OMP 预设则零副作用。

## 5. 缺陷清单

| 缺陷 | 等级 | 状态 |
|---|---|---|
| Gate5 首轮 3 个 TS 错误（OMP 探测结果缺 ProviderModelInfo 字段 + model.input 类型断言） | 轻微（前端） | ✅ 已修复（补字段 + 类型断言，164 全绿） |
| Gate6 首轮 2 个写测试失败（测试写入命中真实 omp 配置目录，污染 ~/.omp/agent/models.yml） | 轻微（测试） | ✅ 已修复（PI_CODING_AGENT_DIR 隔离 + ENV_LOCK 串行化 + 真实文件恢复），技术债登记 TD-004-3 |

无 P0/P1 阻断缺陷。

## 6. 结论

**验收通过（passed）**。DoD 6 项全部核销；核心回归全绿；缺陷 0 阻断；范围决策（结构化模型编辑器 / 仅 models.yml / omp CLI 探测）经用户确认。

**合并决策（用户）**：暂不合并——分支 `feat/CR-004`（498bec8）保留，与 main 上积压的 CR-003 提交（df08602/b391281/b049d3b）一并在后续发布节奏中处理。
